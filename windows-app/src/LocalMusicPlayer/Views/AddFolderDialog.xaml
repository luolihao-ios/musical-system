<Window x:Class="LocalMusicPlayer.Views.AddFolderDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="添加音乐文件夹"
        Width="560"
        Height="210"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Background="#18161B"
        Foreground="#F7F3F4">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="48" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock Text="选择包含本地音乐的文件夹"
                   FontSize="18"
                   FontWeight="SemiBold" />
        <Grid Grid.Row="1"
              Margin="0,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="82" />
            </Grid.ColumnDefinitions>
            <TextBox x:Name="PathTextBox"
                     Padding="10,7"
                     Background="#252129"
                     Foreground="#F7F3F4"
                     BorderThickness="0" />
            <Button Grid.Column="1"
                    Margin="8,0,0,0"
                    Content="浏览…"
                    Click="Browse_Click" />
        </Grid>
        <StackPanel Grid.Row="2"
                    Margin="0,18,0,0"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Width="82"
                    Margin="0,0,8,0"
                    Content="取消"
                    IsCancel="True" />
            <Button Width="82"
                    Background="#E94B5F"
                    Content="开始扫描"
                    Click="Confirm_Click" />
        </StackPanel>
    </Grid>
</Window>
